/**
 * Yönlendirme — gider dönem hesabı ORTAK ÇEKİRDEĞE taşındı (`src/core/expense-view.ts`), çünkü
 * telefonun Giderler ekranı da aynı "bu ay / bu hafta" sınırlarını kullanmak zorunda.
 */
export * from "@/core/expense-view";
